<% if (pr.metaComment) { %>
<!-- <%- JSON.stringify({ "release-pr": { v2: { crates, version } } }) %> -->
<% } %>

This is a release PR for **<%= crate.name %>** version **<%= version.actual %>**<%
    if (version.actual != version.desired) {
%> (performing a(n) <%= version.desired %> bump).<%
    } else {
%>.<%
    }
%>
