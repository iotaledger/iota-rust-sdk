// Copyright (c) 2026 IOTA Stiftung
// SPDX-License-Identifier: Apache-2.0

// Fixture for `ProtoIndex` tests: references messages of another package whose
// simple names collide with its own.

syntax = "proto3";

package fixture.grpc.v1.b;

import "fixture/grpc/v1/nesting_a.proto";

message Input {
  fixture.grpc.v1.a.Input from_a = 1;
  fixture.grpc.v1.a.Argument.Input nested_from_a = 2;
  map<string, uint32> entries = 3;
}
